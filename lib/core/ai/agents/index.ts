// ============================================================
// AI Runtime — Agents Index
// Re-exports all agent base classes.
// Concrete agent implementations are added to this directory
// as the agent layer grows (planner, coach, reflection, etc.).
// ============================================================

export { BaseAgent } from './base.agent';
export type { AgentConfig } from './base.agent';
